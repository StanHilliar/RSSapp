'use strict';

/* jshint -W098 */
angular.module('mean.elocore').controller('ElocoreController', ['$scope', 'Global', 'Elocore',
  function($scope, Global, Elocore) {
    $scope.global = Global;
    $scope.package = {
      name: 'elocore'
    };
  }
]);
